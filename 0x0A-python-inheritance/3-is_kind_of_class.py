#!/usr/bin/python3
"""checks if f the object is an instance of a class"""


def is_kind_of_class(obj, a_class):
    """Write a function that returns True if the object is an instance"""
    if isinstance(obj, a_class):
        return (True)
    return (False)
